<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ohyoyo.persistence.MemberDAO">
	<select id="idOverlap" resultType="int">
		SELECT COUNT(*) FROM tbl_member
		WHERE id = #{id}
	</select>
	<insert id="memInsert">
		INSERT INTO tbl_member(id, pw, name, phone, email, postcode, addr1, addr2, useon, primaryon, snson, emailon)
		VALUES(#{id},#{pw},#{name},#{phone},#{email},#{postcode},#{addr1},#{addr2},#{useon},#{primaryon},#{snson},#{emailon})
	</insert>
	<update id="getKey">
		UPDATE tbl_member
		SET useyn = #{param2}
		WHERE id = #{param1} 
	</update>
	<update id="alterKey">
		UPDATE tbl_member
		SET useyn = 'y'
		WHERE id = #{param1} AND useyn = #{param2}
	</update>
</mapper>